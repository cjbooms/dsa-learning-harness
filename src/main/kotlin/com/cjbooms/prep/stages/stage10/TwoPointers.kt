package com.cjbooms.prep.stages.stage10

/**
 * Stage 10.1 — Two Pointers.
 */

/**
 * Pair sum in a sorted array. Given a strictly increasing `numbers` array,
 * return the 0-based indices `[i, j]` (with `i < j`) of the two numbers that
 * add up to `target`, or `null` if no such pair exists.
 *
 * @param numbers a non-empty array of distinct integers sorted in ascending
 *                order
 * @param target the target sum
 * @return a two-element `IntArray` of the matching indices, or `null` if no
 *         such pair exists
 */
fun pairSumSorted(numbers: IntArray, target: Int): IntArray? {
    TODO("implement")
}

/**
 * Container With Most Water (LC 11). Given an array `heights` where each
 * element represents a vertical line drawn at that index on the x-axis, find
 * two lines that together with the x-axis form a container holding the most
 * water.
 *
 * @param heights an array of non-negative integers; `heights.length >= 2`
 * @return the maximum area of water any pair of lines can contain
 */
fun maxArea(heights: IntArray): Int {
    TODO("implement")
}

/**
 * Remove Duplicates from Sorted Array (LC 26). In-place deduplication of a
 * non-decreasing array. After the call, the first `k` entries of `numbers`
 * must hold the unique values in their original relative order; the remaining
 * entries are unspecified.
 *
 * @param numbers a non-decreasing array to deduplicate in-place
 * @return the number of unique elements (`k`) written to the front of the
 *         array
 */
fun removeDuplicatesSorted(numbers: IntArray): Int {
    TODO("implement")
}
