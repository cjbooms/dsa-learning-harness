package com.cjbooms.prep.stages.stage8

import java.util.ArrayDeque

/**
 * Alien Dictionary.
 *
 * Given a list of words that is sorted according to an unknown alphabet,
 * derive the ordering of the letters in that alphabet.
 *
 * The alphabet uses lowercase letters ('a'..'z'). The derived order must be
 * consistent with the given word ordering; any ordering that is consistent
 * is acceptable.
 *
 * @param words the list of words in alien-alphabet order; non-empty.
 * @return a string containing each distinct letter that appears in [words]
 *   exactly once, in a consistent alien-alphabet order, or an empty string
 *   if no such ordering exists (invalid dictionary or cyclic ordering).
 */
fun alienOrder(words: List<String>): String {
    TODO("implement")
}
